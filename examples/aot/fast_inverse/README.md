Usage:

```bash
bash ./compile.sh  # generate PTO/CPP and build both auto/manual sync libs
python ./run_inverse.py  # default to auto-sync kernel
python ./run_inverse.py --manual-sync  # test manual-sync kernel
```
